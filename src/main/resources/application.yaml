spring:
  application:
    name: projeto
  datasource:
    url: jdbc:mysql://localhost/gestao_projetos
    username: root
    password: root
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        ddl-auto: validate

api:
  security:
    token:
      secret: 123456